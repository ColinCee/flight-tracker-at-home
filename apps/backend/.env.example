# No API credentials needed — airplanes.live is a free, public API.

# Cache TTL in seconds (default: 10s, airplanes.live rate limit ~10s sustained)
# CACHE_TTL=10

# CORS — comma-separated allowed origins
# CORS_ORIGINS=https://flight-tracker-at-home.pages.dev

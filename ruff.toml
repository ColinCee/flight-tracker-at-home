target-version = "py312"
line-length = 88

[lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]  # Redundant with ruff format (which handles code line-length)

[format]
docstring-code-format = true

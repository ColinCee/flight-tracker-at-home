services:
  backend:
    build:
      context: apps/backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
    ports:
      - "8080:8080"
    environment:
      BACKEND_URL: backend:8000
    depends_on:
      - backend
